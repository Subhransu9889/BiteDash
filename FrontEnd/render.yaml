services:
  - type: web
    name: bitedash-app  # or any name
    env: static
    staticPublishPath: build
    buildCommand: npm run build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
